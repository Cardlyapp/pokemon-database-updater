name: Database Update

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

jobs:
  update-db:
    runs-on: ubuntu-latest
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DATABASE_URL_2: ${{ secrets.DATABASE_URL_2 }}
      DATABASE_URLS: ${{ secrets.DATABASE_URLS }}
      POKEMON_API_KEY: ${{ secrets.POKEMON_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt --verbose; fi

      - name: Show installed packages (debug)
        run: |
          echo "--- pip version ---"
          pip --version
          echo "--- pip list ---"
          pip list
      - name: Run card updater
        run: python pokemon-db-updater.py --db-target both --version both
      - name: Run pokedex updater
        run: python pokedex-updater.py
      - name: Run Products updater
        run: python product-updater.py
